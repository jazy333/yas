#LINK_DIRECTORIES()
SET (LIB_TOKENIZER_SRC term.cpp)
add_library(tokenizer SHARED ${LIB_TOKENIZER_SRC})
SET (TEST_TOKENIZER_SRC term.cpp term_test.cpp)
add_executable(tokenizer_test ${TEST_TOKENIZER_SRC}) 
target_link_libraries(tokenizer_test gtest gtest_main)